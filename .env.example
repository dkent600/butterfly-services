# Environment Configuration

# Application Settings
NODE_ENV=development
PORT=3000
HOST=localhost
LOG_LEVEL=info

# TRADING SAFETY: Set to false ONLY when you want REAL trades executed
# Default: true (safe mode - test orders only)
USE_TEST_MODE=true

# Debug Settings (automatically enabled when NODE_ENV=debug)
# DEBUG_MODE=auto        # Enabled when NODE_ENV=debug
# VERBOSE_LOGGING=auto   # Enabled when NODE_ENV=debug
# DISABLE_RATE_LIMITING=auto  # Enabled when NODE_ENV=debug

# MEXC Exchange API Credentials
MEXC_API_KEY=your-mexc-api-key-here
MEXC_API_SECRET=your-mexc-api-secret-here